{
  "name": "koperasi-web",
  "version": "2.0.0",
  "description": "Aplikasi Koperasi Sekolah — Cloudflare Workers + Google Sheets",
  "private": true,
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy --env production",
    "deploy:pages": "npx wrangler pages deploy public --project-name koperasi-web --commit-dirty=true"
  },
  "keywords": ["koperasi", "kasir", "cloudflare-workers", "google-sheets"],
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
